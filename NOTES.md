# Marquee — Internal Notes & Reminders

Small to-dos, deployment steps, and operational reminders that don't belong in the changelog.

---

## Deployment Checklist (Vercel)

- [ ] Add `TMDB_TOKEN` as an environment variable in Vercel project settings
- [ ] Add `NEXT_PUBLIC_SUPABASE_URL` as an environment variable in Vercel project settings
- [ ] Add `NEXT_PUBLIC_SUPABASE_ANON_KEY` as an environment variable in Vercel project settings

---

## Supabase

- [ ] Enable RLS on `movies`, `labels`, and `movie_labels` tables (SQL in chat history from 2026-03-07)

---

## Pending Code Work

See `CHANGELOG.md` `[Unreleased]` section and the **Pending Features / Known Issues** section of `CLAUDE.md`.
